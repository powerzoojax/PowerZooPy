"""SCUC JSON sources and extractors shared with PowerZooJax.

- ``case14.json`` — generator economics / UC scalars (IEEE 14 bus, MATPOWER topology).
- ``scuc_json_to_case_units`` — print unit rows for ``Case14`` / ``create_case14``.

Keep the JSON and script logic aligned with ``powerzoojax/case/raw_cases/`` in the
sibling repository.
"""
